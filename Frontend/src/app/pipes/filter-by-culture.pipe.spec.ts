import { FilterByCulturePipe } from './filter-by-culture.pipe';

describe('FilterByCulturePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCulturePipe();
    expect(pipe).toBeTruthy();
  });
});
